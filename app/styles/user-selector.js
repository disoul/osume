'use strict';
import { StyleSheet } from 'react-native';

const userSelector = StyleSheet.create({ 
  input: {
    width: 200,
    marginRight: 10,
  },
  button: {
    marginLeft: 10,
    backgroundColor: '#ff6666',
  }
});

module.exports = userSelector;
